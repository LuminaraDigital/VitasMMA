import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vitasmma.app',
  appName: 'VitasMMA',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
